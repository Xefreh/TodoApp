<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:layout_margin="16dp">

    <EditText
        android:id="@+id/titleInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:hint="Title" />

    <EditText
        android:id="@+id/bodyInput"
        android:inputType="textMultiLine"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:hint="Body"
        app:layout_constraintTop_toBottomOf="@id/titleInput"
        app:layout_constraintEnd_toEndOf="@id/titleInput"
        app:layout_constraintStart_toStartOf="@id/titleInput" />

    <ImageButton
        android:id="@+id/attachPhotoBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="A camera icon"
        app:layout_constraintTop_toBottomOf="@id/bodyInput"
        app:layout_constraintStart_toStartOf="@id/bodyInput"
        android:src="@drawable/baseline_add_a_photo_24" />

    <Button
        android:id="@+id/saveBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/attachPhotoBtn"
        app:layout_constraintStart_toStartOf="@id/attachPhotoBtn"
        android:text="Save" />

</androidx.constraintlayout.widget.ConstraintLayout>