<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Autorise le trafic HTTP clair uniquement vers le backend local de développement.
        - 10.0.2.2 : alias réseau de la machine hôte depuis l'émulateur Android.
        - localhost / 127.0.0.1 : tests sur appareil physique via adb reverse.
        Tout le reste reste soumis à la politique par défaut (HTTPS requis pour targetSdk 28+).
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
