<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Allows cleartext HTTP traffic only to the local development backend.
        - 10.0.2.2 : network alias of the host machine from the Android emulator.
        - localhost / 127.0.0.1 : tests on a physical device via adb reverse.
        Everything else remains subject to the default policy (HTTPS required for targetSdk 28+).
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
